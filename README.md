# travelSite

# prerequisite
MongoDB
Node

# Run the site
Clone the repo
run the following commands:

npm install
bower install

Start monngoDB
mongod
Now you can start the server with:
nodemon server.js
